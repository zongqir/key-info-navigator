# Tab 切换修复测试说明

## 修复内容概述

我已经修复了 dock 页面在切换 tab 时不跟随更新的问题。主要修复包括：

### 1. 事件监听器扩展
- 添加了更多的 tab 切换相关事件监听：
  - `open-tab` - 打开新 tab 时
  - `close-tab` - 关闭 tab 时  
  - `switch-tab` - 切换 tab 时
  - `layout-tab-switch` - 布局中的 tab 切换
  - `focus-protyle` - 编辑器获得焦点时
  - `open-document` - 打开文档时

### 2. 获取活跃编辑器逻辑优化
- 修复了总是获取第一个编辑器的问题
- 新的逻辑会智能选择当前活跃的编辑器：
  1. 查找具有焦点的编辑器
  2. 查找可见的编辑器
  3. 备选：第一个有效编辑器

### 3. 防抖逻辑改进
- 减少了延迟时间（从 500ms 到 200ms）
- 添加了智能文档切换检测
- 优化了刷新策略（文档切换强制刷新，同文档使用缓存）

## 测试步骤

### 测试 1：基本 Tab 切换
1. 打开 SiYuan，确保插件已启用
2. 打开格式化文本导航 dock（右侧栏）
3. 打开一个包含格式化文本的文档（加粗、高亮、备注等）
4. 确认 dock 显示了该文档的格式化文本
5. **关键测试**：打开另一个文档（新 tab）
6. 检查 dock 是否立即更新显示新文档的内容

### 测试 2：多 Tab 快速切换
1. 打开 3-4 个不同的文档（每个都有不同的格式化文本）
2. 快速在这些 tab 之间切换
3. 观察 dock 是否能够：
   - 及时响应每次切换
   - 显示正确的文档内容
   - 不出现延迟或错误内容

### 测试 3：关闭 Tab 测试
1. 打开多个文档
2. 关闭当前显示在 dock 中的文档的 tab
3. 检查 dock 是否切换到剩余的活跃文档

### 测试 4：调试日志验证
1. 在插件设置中开启调试模式
2. 打开浏览器开发者工具（F12）-> Console
3. 切换 tab 时观察日志输出：
   - 应该看到 "事件触发: xxx" 的日志
   - 应该看到 "检测到文档切换" 的日志
   - 应该看到编辑器选择的相关日志

## 预期结果

✅ **成功的表现：**
- Dock 内容在切换 tab 时立即更新（200ms 内）
- 显示的格式化文本正确对应当前活跃的文档
- 调试日志显示正确的事件触发和文档切换检测

❌ **如果仍有问题：**
- Dock 内容不变或更新缓慢
- 显示的内容与当前文档不匹配
- 控制台出现错误信息

## 调试建议

如果问题仍然存在：

1. **检查事件日志**：确认相关事件是否被触发
2. **验证编辑器获取**：检查是否正确获取到当前活跃编辑器
3. **文档 ID 检查**：确认文档 ID 是否正确变化

## 技术改进说明

这次修复解决了原代码中的关键问题：

1. **编辑器获取问题**：原来使用 `getAllEditor()[0]` 总是获取第一个编辑器，现在会智能选择当前活跃的编辑器
2. **事件覆盖不全**：原来只监听了少数几个事件，现在覆盖了更多 tab 切换相关的事件
3. **响应性能**：优化了防抖逻辑，提高了响应速度和准确性

修复后的代码应该能够正确响应所有 tab 切换场景，让 dock 页面始终显示当前活跃文档的格式化文本。
