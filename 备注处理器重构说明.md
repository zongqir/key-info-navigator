# 备注处理器重构说明

## 🚨 问题分析

### 原设计的问题
1. **过度复杂**：使用了 `getAllMemosFromPage()`、`mergeSimilarMemos()` 等复杂逻辑
2. **不一致**：与其他格式处理器的设计不一致，主要依赖DOM而非数据库
3. **格式理解错误**：没有正确理解思源笔记的备注格式
4. **性能问题**：实时DOM解析影响性能

### 正确的备注格式
**思源笔记的备注格式：** `划线文本(备注内容)`
- **外层**：被标记的文本（显示为划线）
- **括号内**：备注/评论内容

## ✅ 重构方案

### 1. 数据库优先策略
```typescript
// 像其他格式一样，主要通过spans表查询
SELECT * FROM spans 
WHERE root_id = "文档ID" 
  AND type = "inline-memo"
ORDER BY block_id, start_offset;
```

### 2. 正确的数据结构理解
```typescript
interface MemoSpan {
    content: string;        // 被标记的文本（划线部分）
    memo_content: string;   // 备注内容（括号内容）
    type: "inline-memo";    // 固定类型
}
```

### 3. 简化的处理逻辑
```typescript
public extractFromSpan(span: any, blockId?: string): FormattedTextItem[] {
    const markedText = span.content;      // 被标记的文本
    const memoContent = span.memo_content; // 备注内容
    
    return [{
        text: markedText,           // 显示被标记的文本
        memoContent: memoContent,   // 备注内容
        // ... 其他标准字段
    }];
}
```

## 🎨 UI渲染改进

### 新的渲染结构
```html
<div class="formatted-text-dock__memo-container">
    <span class="formatted-text-dock__memo-marked-text">被标记的文本</span>
    <span class="formatted-text-dock__memo-separator"> → </span>
    <span class="formatted-text-dock__memo-content">备注内容</span>
</div>
```

### CSS样式支持
```scss
.formatted-text-dock {
    &__memo-container {
        display: flex;
        align-items: center;
        gap: 6px;
        width: 100%;
    }
    
    &__memo-marked-text {
        font-weight: 500;
        color: var(--kinfo-text-primary);
        text-decoration: underline;
        text-decoration-color: var(--kinfo-primary);
    }
    
    &__memo-separator {
        color: var(--kinfo-text-tertiary);
        font-size: 12px;
    }
    
    &__memo-content {
        color: var(--kinfo-text-tertiary);
        font-style: italic;
        font-size: 11px;
        flex: 1;
        min-width: 0;
        word-break: break-word;
    }
}
```

## 🔧 核心改进

### 1. 移除复杂方法
- ❌ `getAllMemosFromPage()` - 不再需要
- ❌ `mergeSimilarMemos()` - 不再需要  
- ❌ `findParentBlock()` - 使用标准方法
- ❌ `groupMemosByBlock()` - 使用通用分组

### 2. 标准化数据提取
```typescript
// 旧方式：复杂的DOM遍历
const memoElements = container.querySelectorAll('[data-type*="inline-memo"]');
// 处理合并、位置计算等复杂逻辑...

// 新方式：标准的span处理
public extractFromSpan(span: any): FormattedTextItem[] {
    return this.createStandardMemoItem(span);
}
```

### 3. 一致的架构
```typescript
// 与其他处理器保持一致的接口
class MemoProcessor extends BaseFormatProcessor {
    extractFromSpan()    // ✅ 标准方法
    extractFromHTML()    // ✅ 标准方法  
    renderMainContent()  // ✅ 自定义渲染
    renderItemDetails()  // ✅ 详情渲染
}
```

## 📊 性能提升

### 数据库查询优化
- **旧方式**：DOM遍历 + 复杂位置计算 = 慢
- **新方式**：SQL查询 + 标准处理 = 快

### 内存使用优化
- **旧方式**：存储DOM元素引用 + 复杂对象
- **新方式**：标准FormattedTextItem结构

## 🧪 测试验证

### 1. 数据库查询测试
```sql
-- 验证备注数据结构
SELECT id, content, memo_content, type, block_id 
FROM spans 
WHERE type = 'inline-memo' 
LIMIT 5;
```

### 2. 格式解析测试
```typescript
// 测试标记文本和备注内容的正确提取
const testSpan = {
    content: "重要文本",
    memo_content: "这是备注内容",
    type: "inline-memo"
};

const result = memoProcessor.extractFromSpan(testSpan);
console.log(result[0].text);         // "重要文本"
console.log(result[0].memoContent);  // "这是备注内容"
```

## 🔄 迁移步骤

### 1. 替换处理器文件
```bash
# 备份原文件
mv MemoProcessor.ts MemoProcessor.old.ts
# 使用新文件
mv MemoProcessor-Refactored.ts MemoProcessor.ts
```

### 2. 更新CSS样式
添加新的备注样式到 `src/styles/dock-components.scss`

### 3. 移除特殊处理逻辑
在 `TextFormatParser.ts` 中移除备注的特殊处理：
```typescript
// 移除这个特殊分支
if (options.enabledFormats.includes(TextFormatType.MEMO)) {
    return this.extractWithMemoSupport(...);
}
```

### 4. 验证功能
- ✅ 备注能正确从数据库查询
- ✅ UI正确显示：标记文本 → 备注内容
- ✅ 点击导航正常工作
- ✅ 操作按钮（添加备注、删除格式）正常

## 🎯 预期效果

重构后的备注处理器将：
- ✅ **简洁一致**：与其他格式处理器保持一致的架构
- ✅ **性能更好**：基于数据库查询，避免复杂DOM操作
- ✅ **正确理解**：准确处理思源笔记的备注格式
- ✅ **易于维护**：标准化的代码结构，易于理解和修改
- ✅ **功能完整**：保持所有原有功能，UI更清晰

这样的重构让备注处理回归简单和高效！
