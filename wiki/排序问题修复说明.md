# 备注排序问题修复说明

## 问题分析

### 根本原因
1. **双重提取机制**：
   - 备注使用 `getAllMemosFromPage()` 直接从真实DOM提取
   - 其他格式使用 `extractFromHTML()` 从HTML字符串重建DOM后提取
   - 导致两种提取方式使用不同的DOM基准

2. **位置计算不一致**：
   - 备注：基于真实DOM的 `getElementDocumentPosition()`
   - 其他格式：基于重建DOM的位置计算
   - 同一文档中不同格式的位置计算基准不同

3. **合并逻辑差异**：
   - 备注有特殊的 `mergeSimilarMemos()` 合并逻辑
   - 其他格式没有合并逻辑
   - 最终排序时混合了已合并和未合并的项目

## 解决方案

### 核心改进
1. **统一DOM提取方法**：
   - 创建 `extractFromDOMUnified()` 方法
   - 所有格式化文本都从同一个DOM容器提取
   - 避免重复处理同一个元素

2. **统一位置计算**：
   - 创建 `calculateUnifiedPosition()` 方法
   - 所有格式化文本使用相同的位置计算基准
   - 确保排序的一致性

3. **改进的处理器接口**：
   - 为所有处理器添加 `extractFromElement()` 公共方法
   - 备注处理器重写此方法以保持特殊逻辑
   - 统一调用接口

### 修改的文件
1. `src/modules/formatProcessor/TextFormatParser.ts`
   - 重写 `extractWithMemoSupport()` 方法
   - 添加统一的DOM提取和位置计算逻辑
   - 增加详细的调试日志

2. `src/modules/formatProcessor/BaseFormatProcessor.ts`
   - 添加 `extractFromElement()` 公共方法
   - 增加位置计算的调试日志

3. `src/modules/formatProcessor/processors/MemoProcessor.ts`
   - 重写 `extractFromElement()` 方法
   - 增加位置计算的调试日志

## 调试功能

### 新增的调试日志
- 🚀 统一DOM提取方法启动
- 🔍 各处理器找到的元素数量
- 📍 统一位置计算结果
- 🔍 提取结果和最终排序结果

### 使用方法
1. 打开浏览器开发者工具
2. 切换到Console标签
3. 刷新页面或触发格式化文本更新
4. 查看带有以上表情符号的日志信息

## 验证修复效果

### 预期结果
1. 备注和其他格式化文本的排序应该与文档中的实际位置一致
2. 不再出现备注排序异常的情况
3. 调试日志显示位置计算的一致性

### 如果问题仍然存在
1. 检查调试日志中的位置计算结果
2. 确认不同类型格式化文本的位置数值是否合理
3. 检查是否有其他排序逻辑影响最终结果

## 后续优化建议

1. 考虑完全统一所有格式化文本的提取机制
2. 优化位置计算性能，避免查询所有元素
3. 增加单元测试来验证排序逻辑的正确性
