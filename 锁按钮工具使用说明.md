# 锁按钮工具类使用说明

**更新日期**: 2025-10-02  
**版本**: v1.3.0

## 🎯 功能概述

提供统一的、准确的"获取当前激活tab的锁按钮"和"判断文档只读状态"的工具类。

### ✨ 核心特性

1. **多策略查找** - 4层查找策略，确保准确性
2. **统一接口** - 消除重复代码，易于维护
3. **调试友好** - 详细的日志输出，支持控制台调试
4. **向下兼容** - 保留旧的 `DocumentReadonlyChecker` 接口

---

## 📦 模块结构

```
src/modules/utils/
├── ReadonlyButtonUtils.ts      【新增】核心工具函数
├── DocumentReadonlyChecker.ts  【重构】监听器版本（现在内部使用 ReadonlyButtonUtils）
└── index.ts                    【更新】导出所有工具
```

---

## 🔧 API 参考

### 1. `getCurrentActiveReadonlyButton()`

获取当前激活文档的锁按钮（多策略查找）

**函数签名**:
```typescript
function getCurrentActiveReadonlyButton(): HTMLElement | null
```

**返回值**:
- `HTMLElement` - 找到的锁按钮元素
- `null` - 未找到锁按钮

**查找策略**（按优先级）:
1. 🥇 **优先**: 通过思源 `getActiveTab()` API 获取（最准确）
2. 🥈 **次选**: 通过焦点元素 `document.activeElement` 向上查找
3. 🥉 **备选**: 查找活跃窗口 `.layout__wnd--active`
4. 🆘 **兜底**: 全局查找第一个（可能不准确，会有警告日志）

**使用示例**:
```typescript
import { getCurrentActiveReadonlyButton } from '@/modules/utils';

const readonlyBtn = getCurrentActiveReadonlyButton();
if (readonlyBtn) {
    console.log('找到锁按钮:', readonlyBtn);
    const iconHref = readonlyBtn.querySelector('use')?.getAttribute('xlink:href');
    console.log('图标:', iconHref);
} else {
    console.log('未找到锁按钮');
}
```

---

### 2. `isCurrentDocumentReadonly()`

检查当前文档是否只读（已锁定）

**函数签名**:
```typescript
function isCurrentDocumentReadonly(): boolean
```

**返回值**:
- `true` - 只读模式（已锁定🔒）
- `false` - 可编辑模式（已解锁✏️）或未找到锁按钮

**判断逻辑**（基于思源源码）:
```typescript
// 解锁状态的特征：
// 1. data-subtype="unlock" → 已解锁（可编辑）
// 2. aria-label 包含 "取消" → 已解锁（"取消临时解锁"）
// 3. 图标是 #iconUnlock → 已解锁

const isReadonly = !(
    dataSubtype === 'unlock' || 
    ariaLabel.includes('取消') ||
    iconHref === '#iconUnlock'
);
```

**使用示例**:
```typescript
import { isCurrentDocumentReadonly } from '@/modules/utils';

if (isCurrentDocumentReadonly()) {
    console.log('🔒 当前文档已锁定（只读模式）');
    // 可以显示高亮工具栏、标签面板等
} else {
    console.log('✏️ 当前文档可编辑（已解锁）');
    // 隐藏工具栏，阻止操作
}
```

---

### 3. `isCurrentDocumentEditable()`

检查当前文档是否可编辑（已解锁）

**函数签名**:
```typescript
function isCurrentDocumentEditable(): boolean
```

**返回值**:
- `true` - 可编辑（已解锁🔓）
- `false` - 只读（已锁定🔒）或未找到锁按钮

**使用示例**:
```typescript
import { isCurrentDocumentEditable } from '@/modules/utils';

// 兜底防御：执行写入操作前检查
if (isCurrentDocumentEditable()) {
    console.error('🛡️ 兜底防御：文档可编辑，拒绝操作');
    throw new Error('文档未锁定，禁止执行写入操作');
}

// 执行写入操作...
```

---

### 4. `getDocumentStatusText()`

获取用户友好的文档状态描述

**函数签名**:
```typescript
function getDocumentStatusText(): string
```

**返回值**:
- `"文档已锁定🔒（只读模式）"` - 只读状态
- `"文档已解锁✏️（可编辑）"` - 可编辑状态

**使用示例**:
```typescript
import { getDocumentStatusText } from '@/modules/utils';

const statusText = getDocumentStatusText();
console.log(statusText); // "文档已锁定🔒（只读模式）"
```

---

### 5. `getDocumentStatusDetail()`

获取当前文档的详细状态信息（用于调试）

**函数签名**:
```typescript
function getDocumentStatusDetail(): {
    hasButton: boolean;        // 是否找到锁按钮
    isReadonly: boolean;       // 是否只读
    isEditable: boolean;       // 是否可编辑
    ariaLabel: string;         // aria-label 属性
    dataSubtype: string;       // data-subtype 属性
    iconHref: string;          // 图标 xlink:href
    statusText: string;        // 状态描述文本
}
```

**使用示例**:
```typescript
import { getDocumentStatusDetail } from '@/modules/utils';

const detail = getDocumentStatusDetail();
console.log('文档状态详情:', detail);
// {
//   hasButton: true,
//   isReadonly: true,
//   isEditable: false,
//   ariaLabel: "临时锁定",
//   dataSubtype: "lock",
//   iconHref: "#iconLock",
//   statusText: "文档已锁定🔒（只读模式）"
// }
```

---

## 🎨 使用场景

### 场景1: 显示工具栏前检查

```typescript
import { getCurrentActiveReadonlyButton } from '@/modules/utils';

// 在显示工具栏前检查只读状态
const handleSelection = () => {
    const readonlyBtn = getCurrentActiveReadonlyButton();
    
    if (!readonlyBtn) {
        return; // 未找到锁按钮，不显示工具栏
    }
    
    const iconHref = readonlyBtn.querySelector('use')?.getAttribute('xlink:href') || '';
    const isDocReadonly = iconHref !== '#iconUnlock';
    
    if (!isDocReadonly) {
        return; // 文档可编辑，不显示工具栏
    }
    
    // 显示工具栏...
};
```

### 场景2: 点击高亮/标签前检查

```typescript
import { isCurrentDocumentReadonly } from '@/modules/utils';

// 点击高亮文本时检查
const showDeleteDialog = (highlightElement: HTMLElement) => {
    const isDocReadonly = isCurrentDocumentReadonly();
    
    if (!isDocReadonly) {
        Logger.log('⛔ 文档未锁定，不显示删除对话框');
        return;
    }
    
    Logger.log('✅ 文档已锁定，显示删除对话框');
    // 显示删除对话框...
};
```

### 场景3: 执行写入操作前的兜底防御

```typescript
import { isCurrentDocumentEditable, getCurrentActiveReadonlyButton } from '@/modules/utils';

// 强制解锁前的兜底检查
const forceUnlock = async () => {
    // 兜底防御
    if (isCurrentDocumentEditable()) {
        Logger.error('🛡️ 兜底防御：文档可编辑，拒绝操作');
        throw new Error('文档未锁定，禁止操作');
    }
    
    // 获取锁按钮并点击解锁
    const readonlyBtn = getCurrentActiveReadonlyButton();
    if (!readonlyBtn) {
        throw new Error('未找到锁按钮');
    }
    
    readonlyBtn.click();
    await new Promise(resolve => setTimeout(resolve, 100));
};
```

---

## 🎮 控制台调试

插件启动后，会在控制台暴露全局对象 `KeyInfoNavigator`，可以直接调试：

### 文档状态检查命令

```javascript
// 检查当前文档只读状态（详细信息）
KeyInfoNavigator.checkDocStatus()

// 当前文档是否只读
KeyInfoNavigator.isReadonly()

// 当前文档是否可编辑
KeyInfoNavigator.isEditable()

// 获取当前文档的锁按钮
KeyInfoNavigator.getReadonlyButton()
```

### 调试日志控制命令

```javascript
// 开启调试日志（会输出详细的查找过程）
KeyInfoNavigator.enableDebug()

// 关闭调试日志
KeyInfoNavigator.disableDebug()

// 切换调试模式
KeyInfoNavigator.toggleDebug()

// 查看当前状态
KeyInfoNavigator.getDebugStatus()
```

---

## 📝 调试技巧

### 1. 开启调试日志查看查找过程

```javascript
// 1. 开启调试日志
KeyInfoNavigator.enableDebug()

// 2. 检查文档状态（会输出详细的查找过程）
KeyInfoNavigator.checkDocStatus()

// 3. 关闭调试日志
KeyInfoNavigator.disableDebug()
```

**输出示例**:
```
🔍 [ReadonlyButton] 开始查找当前活跃文档的锁按钮...
🔍 [ReadonlyButton] 思源getActiveTab返回: { hasActiveTab: true, tabId: "xxx" }
✅ [ReadonlyButton] 策略1成功 - 通过getActiveTab找到锁按钮: { iconHref: "#iconUnlock" }
🔍 [ReadonlyButton] 文档状态检查: {
  aria-label: "取消临时解锁",
  data-subtype: "unlock",
  图标href: "#iconUnlock",
  是否解锁: "✏️ 是（可编辑）",
  是否只读: "✏️ 否（解锁）"
}
```

### 2. 查看多tab场景下的查找策略

```javascript
// 1. 开启调试日志
KeyInfoNavigator.enableDebug()

// 2. 切换不同的tab
// 3. 每次切换后检查
KeyInfoNavigator.checkDocStatus()

// 观察使用了哪个策略（策略1/2/3/4）
```

### 3. 手动获取锁按钮并检查属性

```javascript
// 获取锁按钮
const btn = KeyInfoNavigator.getReadonlyButton()

// 输出：
// ✅ 找到锁按钮: <button>...</button>
// 按钮属性: {
//   ariaLabel: "临时锁定",
//   dataSubtype: "lock",
//   iconHref: "#iconLock"
// }
```

---

## 🔄 迁移指南

### 从旧的实现迁移

#### 旧代码（❌ 不推荐）:
```typescript
// 各个文件中重复的实现
private getCurrentActiveReadonlyButton(): HTMLElement | null {
    const readonlyBtn = document.querySelector('.protyle-breadcrumb button[data-type="readonly"]');
    return readonlyBtn as HTMLElement;
}

private checkDocumentReadonly(): boolean {
    const readonlyBtn = this.getCurrentActiveReadonlyButton();
    if (!readonlyBtn) return false;
    
    const iconHref = readonlyBtn.querySelector('use')?.getAttribute('xlink:href') || '';
    return iconHref !== '#iconUnlock';
}
```

#### 新代码（✅ 推荐）:
```typescript
import { getCurrentActiveReadonlyButton, isCurrentDocumentReadonly } from '@/modules/utils';

// 直接使用统一的工具函数
const readonlyBtn = getCurrentActiveReadonlyButton();
const isReadonly = isCurrentDocumentReadonly();
```

### 向下兼容

如果你的代码使用了 `DocumentReadonlyChecker`，无需修改，它现在内部使用新的工具类：

```typescript
import { DocumentReadonlyChecker } from '@/modules/utils';

// 仍然可以使用（向下兼容）
const isReadonly = DocumentReadonlyChecker.checkDocumentReadonly();

// 但建议改为直接使用新工具（更简洁）
import { isCurrentDocumentReadonly } from '@/modules/utils';
const isReadonly = isCurrentDocumentReadonly();
```

---

## 📊 多策略查找的优势

### 为什么需要多策略？

在思源笔记的多tab场景下，简单的 `document.querySelector()` 可能拿到错误的tab的锁按钮。

#### ❌ 简单实现的问题:
```typescript
// 可能拿到第一个tab的锁按钮，而不是当前激活的tab
const readonlyBtn = document.querySelector('.protyle-breadcrumb button[data-type="readonly"]');
```

#### ✅ 多策略查找的优势:
```typescript
// 策略1: 优先使用思源API（最准确）
const activeTab = siyuan.getActiveTab();
const readonlyBtn = activeTab.model.editor.protyle.element.querySelector('...');

// 策略2: 通过焦点元素查找（次选）
const protyleContainer = document.activeElement.closest('.protyle');
const readonlyBtn = protyleContainer.querySelector('...');

// 策略3: 查找活跃窗口（备选）
const activeWnd = document.querySelector('.layout__wnd--active');
const readonlyBtn = activeWnd.querySelector('...');

// 策略4: 兜底方案（会有警告）
const readonlyBtn = document.querySelector('...');
```

---

## 🎓 最佳实践

### 1. 优先使用函数式API

```typescript
// ✅ 推荐：直接使用函数
import { isCurrentDocumentReadonly } from '@/modules/utils';
const isReadonly = isCurrentDocumentReadonly();

// ❌ 不推荐：使用类方法（多了一层调用）
import { DocumentReadonlyChecker } from '@/modules/utils';
const isReadonly = DocumentReadonlyChecker.checkDocumentReadonly();
```

### 2. 需要监听状态变化时使用 DocumentReadonlyChecker

```typescript
import { DocumentReadonlyChecker } from '@/modules/utils';

// 添加状态变化监听
DocumentReadonlyChecker.addStateChangeListener((isReadonly) => {
    console.log('文档状态变化:', isReadonly ? '🔒 锁定' : '✏️ 解锁');
    // 更新UI...
});
```

### 3. 调试时开启日志

```typescript
// 在开发环境或调试时开启
if (process.env.NODE_ENV === 'development') {
    Logger.setDebugEnabled(true);
}

// 或者在控制台手动开启
// KeyInfoNavigator.enableDebug()
```

---

## 🐛 常见问题

### Q1: 为什么有时候拿不到锁按钮？

**A**: 可能的原因：
1. 当前没有打开的文档
2. 面包屑尚未渲染完成
3. 在特殊的视图下（如搜索、数据库等）

**解决方法**: 总是检查返回值是否为 `null`

```typescript
const readonlyBtn = getCurrentActiveReadonlyButton();
if (!readonlyBtn) {
    Logger.warn('未找到锁按钮，可能当前没有打开文档');
    return;
}
```

### Q2: 多tab场景下如何确保准确？

**A**: 使用多策略查找，优先使用思源 `getActiveTab()` API：

```typescript
// 工具类已经实现了多策略查找，直接使用即可
const readonlyBtn = getCurrentActiveReadonlyButton();

// 如果开启调试日志，可以看到使用了哪个策略
KeyInfoNavigator.enableDebug();
KeyInfoNavigator.getReadonlyButton();
```

### Q3: 如何判断解锁状态？

**A**: 基于思源源码，解锁状态有3个特征：

```typescript
// 1. data-subtype="unlock"
// 2. aria-label 包含 "取消"
// 3. 图标是 #iconUnlock

const isUnlocked = 
    dataSubtype === 'unlock' || 
    ariaLabel.includes('取消') ||
    iconHref === '#iconUnlock';
```

---

## 📚 相关文档

- [控制台调试说明.md](./控制台调试说明.md) - 调试日志功能说明
- [状态识别时机汇总.md](./状态识别时机汇总.md) - 各个位置如何检测状态
- [重构说明.md](./重构说明.md) - 重构统一锁按钮工具的说明

---

## 🎉 总结

这个工具类提供了：

✅ **统一的接口** - 消除重复代码，易于维护  
✅ **准确的查找** - 多策略查找，确保在多tab场景下正确  
✅ **调试友好** - 详细的日志输出，支持控制台调试  
✅ **向下兼容** - 保留旧接口，平滑迁移  
✅ **最佳实践** - 函数式API，简洁易用  

现在你可以在任何地方使用这些工具函数，不再需要重复实现锁按钮查找逻辑了！🎊

